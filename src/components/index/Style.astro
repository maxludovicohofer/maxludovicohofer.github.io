---
import Post from "@layouts/Post.astro";
import { getEntry } from "astro:content";

const post = await getEntry("thoughts", "style");

const { Content } = await post.render();
---

<Post>
  <Fragment slot="title">{post.data.title}</Fragment>
  <Content />
</Post>
