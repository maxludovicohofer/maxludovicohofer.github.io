---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"span"> {}

const { ...props } = Astro.props;

const isKatex = props.class === "katex";
---

<span {...props} {...isKatex ? { translate: "no" } : {}}><slot /></span>
