---
import type { ComponentProps } from "astro/types";
import Text from "../Text.astro";

interface Props extends ComponentProps<typeof Text> {}

const { ...props } = Astro.props;
---

<Text tag="ul" {...props}><slot /></Text>
