---
import {
  getTextClass,
  type TextTag,
  type TextSize,
} from "@integrations/tailwind";
import type { HTMLAttributes } from "astro/types";

interface Props extends Omit<HTMLAttributes<TextTag>, "title"> {
  title?: boolean;
  subtitle?: boolean;
  markdown?: boolean;
  tag?: TextTag | undefined;
  importance?: TextSize | undefined;
}

const {
  class: className = "",
  title,
  subtitle,
  markdown,
  tag,
  importance,
  ...props
} = Astro.props;

const Element = tag ?? (markdown ? "div" : title ? "h1" : "p");

if (markdown) import("katex/dist/katex.min.css");
---

<Element
  class={`${className} ${getTextClass(markdown ? "markdown" : (importance ?? (subtitle ? "small" : Element)), title ? "heading" : subtitle ? "detail" : undefined)}`}
  {...props}><slot /></Element
>
