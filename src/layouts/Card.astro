---
interface Props {
  className?: string;
}

const { className } = Astro.props;

const finalClass = `rounded-3xl bg-zinc-100 dark:bg-zinc-900 shadow-xl dark:shadow-dark p-8 2xl:p-12 overflow-hidden ${className ?? ""}`;
---

<div class={finalClass}>
  <slot />
</div>
