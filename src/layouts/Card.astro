---
interface Props {
  className?: string;
}

const { className = "" } = Astro.props;
---

<div
  class={`rounded-3xl bg-zinc-100 dark:bg-zinc-900 shadow-md overflow-hidden relative group p-8 2xl:p-12 ${className}`}
>
  <slot />
</div>
