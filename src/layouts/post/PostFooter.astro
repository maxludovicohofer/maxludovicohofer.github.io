---
import Text from "@ui/Text.astro";

interface Props {
  value: string | string[];
}

const { value } = Astro.props;
---

<Text tag="b" class="font-normal"><slot /></Text>
{Array.isArray(value) ? value.join(", ") : value}
